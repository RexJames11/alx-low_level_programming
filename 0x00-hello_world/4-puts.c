#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'Using the puts() function'
 *
 * Return: Always 0
 */

int main(void)
{
	char *pro = ("\"Programming is like building a multilingual puzzle");

	puts(pro);
	return (0);
}
