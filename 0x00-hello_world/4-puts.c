#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'Using the puts() function'
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char build[] = "Programming is like building a multilingual puzzle\n";

	puts(build);

	return (0);
}
